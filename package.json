{
  "name": "bankql",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "azurite": "mkdir -p .azurite && azurite --silent --location .azurite"
  },
  "devDependencies": {
    "azurite": "^3.34.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "npm@11.12.1",
  "engines": {
    "node": ">=24"
  }
}
